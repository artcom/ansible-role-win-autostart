galaxy_info:
  author: ac
  description: create an autostart link for a command on a Windows machine
  license: license (MIT)

  min_ansible_version: 2.9
  platforms:
    - name: Windows
      versions:
        - "10"

  galaxy_tags: []

dependencies:
  - src: git@gitlab.artcom.de:ansible-roles/win-reboot-handler.git
    scm: git
    name: reboot-handler
    version: "1.0.0"
